# Runtime switches
EXPO_PUBLIC_USE_MOCK=false

# Base URL for server APIs (only used when USE_MOCK=false)
# Leave unset to use https://api.dawn-eat.com by default
# EXPO_PUBLIC_API_URL=https://danshi.fdueat.com/api/v1

# Optional request timeout (ms)
# EXPO_PUBLIC_REQUEST_TIMEOUT_MS=10000